#include <stdio.h>
#include <stdlib.h>

int main() {
float cavalos;
printf("Quantos cavalos voce possui:");
scanf("%f",&cavalos);
printf("Voce precisa de %.2f ferraduras para equipar todos os cavalos hospedados.", cavalos);
return 0;
}
